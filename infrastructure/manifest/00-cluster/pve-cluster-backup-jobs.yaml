pve_cluster_backup_jobs:
  daily-backup:
    schedule: "*-*-* 23:00"
    storage: pbs-primary
    mode: snapshot
    compress: zstd
    # Throttle host CPU/IO so the vzdump doesn't starve etcd on the shared node
    zstd: 1
    max_workers: 2
    ionice: 8
    vmids:
      - 1100 # windows_01
