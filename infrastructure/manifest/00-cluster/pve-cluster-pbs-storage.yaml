## Password: tfvars `pbs_user_passwords.<username>`, shared with the 60-pbs user
pve_cluster_pbs_storage:
  pbs-primary:
    pbs:
      server: "backup-01.zimmermann.eu.com"
      username: "backup"
      datastore: "datastore-primary"
  # nodes: [pve-1, pve-2]  # optional, empty = all nodes
