ci_user_config:
  ## Standard Linux VM
  std_linux:
    - username: alexander
      groups: [sudo]
      ssh_public_key: ~/.ssh/id_rsa.pub
      set_password: false
      password: ""

  ## Cluster management VM (Omni)
  cluster_mgmt:
    - username: omni
      groups: [sudo, docker]
      set_password: false
      password: ""

    - username: alexander
      groups: [sudo, docker, omni]
      ssh_public_key: ~/.ssh/id_rsa.pub
      set_password: false
      password: ""

  ## Proxmox Backup Server VM
  proxmox_backup:
    - username: alexander
      groups: [sudo]
      ssh_public_key: ~/.ssh/id_rsa.pub
      set_password: false
      password: ""

    ## Remote-exec user of the 60-pbs layer, same key as on the PVE nodes
    - username: opentofu
      groups: [sudo]
      ssh_public_key: ~/.ssh/proxmox_opentofu.pub
      set_password: false
      password: ""

  ## UPS Monitor VM
  ups_monitor:
    - username: alexander
      groups: [sudo]
      ssh_public_key: ~/.ssh/id_rsa.pub
      set_password: false
      password: ""
