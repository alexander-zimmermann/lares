apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool

# One pool for every LoadBalancer Service (no serviceSelector). Each overlay injects
# the `.200`-`.254` block of its /24: VLAN 10 for prod, dev's own routed prefix.
metadata:
  name: default-bgp-ip-pool

spec:
  blocks:
    - start: PLACEHOLDER
      stop: PLACEHOLDER
