apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: hermes-home
  namespace: agents

spec:
  accessModes:
    - ReadWriteOnce
  # Retained and backed up daily: sessions, memory and the Codex login live here.
  storageClassName: longhorn-backup
  resources:
    requests:
      storage: PLACEHOLDER
