apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: hermes-data
  namespace: agents

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: PLACEHOLDER
