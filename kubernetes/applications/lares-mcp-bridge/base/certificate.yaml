apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: lares-mcp-bridge-tls-cert
  namespace: lares-mcp-bridge

spec:
  secretName: lares-mcp-bridge-tls-cert
  issuerRef:
    name: letsencrypt-dns01-issuer
    kind: ClusterIssuer
  dnsNames:
    - PLACEHOLDER
