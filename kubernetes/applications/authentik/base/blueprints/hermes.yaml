version: 1
metadata:
  name: hermes-oidc
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  # OAuth2/OIDC Provider for the Hermes dashboard: a public PKCE client, the
  # only kind its self-hosted OIDC login supports (no client secret).
  - model: authentik_providers_oauth2.oauth2provider
    state: present
    identifiers:
      name: hermes
    attrs:
      name: hermes
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: public
      # Providers created after authentik migration 0032 default to an empty list,
      # which rejects every authorize request as invalid_request.
      grant_types:
        - authorization_code
        - refresh_token
      client_id: hermes-dashboard
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      redirect_uris:
        - matching_mode: strict
          url: !Env AUTHENTIK_HERMES_REDIRECT_URI
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]

  # Application entry in Authentik's app library
  - model: authentik_core.application
    state: present
    identifiers:
      slug: hermes
    attrs:
      name: Hermes
      slug: hermes
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, hermes]]
      policy_engine_mode: any

  # Restrict the dashboard to platform-admins
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !Find [authentik_core.application, [slug, hermes]]
      group: !Find [authentik_core.group, [name, platform-admins]]
      order: 0
    attrs:
      enabled: true
