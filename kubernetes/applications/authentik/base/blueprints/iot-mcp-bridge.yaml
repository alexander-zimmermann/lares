version: 1
metadata:
  name: iot-mcp-bridge-oidc
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  # OAuth2/OIDC Provider for the iot-mcp-bridge MCP server.
  # Used by claude.ai's custom-connector OAuth flow (oauth_anthropic_creds).
  - model: authentik_providers_oauth2.oauth2provider
    state: present
    identifiers:
      name: iot-mcp-bridge
    attrs:
      name: iot-mcp-bridge
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-explicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: confidential
      client_id: iot-mcp-bridge
      client_secret: !Env IOT_MCP_BRIDGE_OIDC_CLIENT_SECRET
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      access_token_validity: hours=1
      refresh_token_validity: days=30
      redirect_uris:
        - matching_mode: strict
          url: !Env ANTHROPIC_OAUTH_REDIRECT_URI
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
        # Without offline_access authentik issues no refresh token and the connector dies with the access token
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-offline_access]]

  # Application entry in Authentik's app library
  - model: authentik_core.application
    state: present
    identifiers:
      slug: iot-mcp-bridge
    attrs:
      name: iot-mcp-bridge
      slug: iot-mcp-bridge
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, iot-mcp-bridge]]
      policy_engine_mode: any

  # Restrict token issuance for this audience to platform-admins
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !Find [authentik_core.application, [slug, iot-mcp-bridge]]
      group: !Find [authentik_core.group, [name, platform-admins]]
      order: 0
    attrs:
      enabled: true
